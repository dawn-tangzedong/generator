package ${packagePath}.${moduleName}.service;

import org.springframework.stereotype.Service;
#if(${baseEntityConfig.exist})
import ${baseEntityConfig.packageRelativePath};
#else
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
#end
import ${packagePath}.${moduleName}.dao.${tableInfo.upperClassName}Dao;
import ${packagePath}.${moduleName}.domain.${tableInfo.upperClassName};

/**
 * @author ${author}
 * @apiNote $!{tableInfo.comments}service实现
 * @since ${datetime}
 */
@Service
public class ${tableInfo.upperClassName}Service extends#if(${baseEntityConfig.exist}) ${baseEntityConfig.className}#else ServiceImpl#end<${tableInfo.upperClassName}Dao, ${tableInfo.upperClassName}> {

}
